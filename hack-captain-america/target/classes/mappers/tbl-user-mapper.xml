<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.captain_america.mapper.TblUserMapper">

    <resultMap id="TblUserDOResult" type="TblUserDO">
        <result property="id" column="id"/>
        <result property="createTime" column="create_time"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
    </resultMap>

    <sql id="TblUserDOFields">
        id,create_time,username,password,email
    </sql>

    <select id="queryById" resultMap="TblUserDOResult" parameterType="Long">
        SELECT
        <include refid="TblUserDOFields"/>
        FROM tbl_user
        WHERE id=#{id}
    </select>

</mapper>